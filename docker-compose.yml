version: '3.8'

services:
  api:
    build: ./backend
    container_name: honeywell_security_api
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENVIRONMENT=production
    restart: always
